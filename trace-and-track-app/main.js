/* import { startProgram } from './menu.js';

startProgram();
 */

// Import functions from the helper module
const { startProgram } = require('./menu');

// Call the greet function from the helper module

startProgram();